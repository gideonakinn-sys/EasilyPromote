// The creator dashboard layout renders the profile for this route (CreatorShell shows ProfileView
// when the path is /dashboard/creator/profile), so the page itself has nothing to add.
export default function CreatorProfilePage() {
  return null;
}
